{
  "name": "@caretv/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@caretv/adapters": "workspace:*",
    "@caretv/config": "workspace:*",
    "@caretv/core": "workspace:*",
    "@caretv/database": "workspace:*",
    "@caretv/state-machine": "workspace:*"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
